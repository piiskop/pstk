/**
 * 
 */
var prompting = false;
if (prompting)
{
	var entry = prompt("What exactly do I want to learn about JS?");
	
	if(entry)
		{
		document.write(entry); //kuvab selle, mida kasutaja sisestas
		}
		
}